# apps/

- `mission-control/` — **P3** — local Next.js dashboard: trend feed →
  script editor (per-scene regenerate) → render preview → Review Gate →
  publish queue → calendar. Framer-motion UI. The approve button logs the
  human edit pass on every video.
